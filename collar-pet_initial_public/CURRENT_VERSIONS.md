# Current prototype versions

Initial public repository snapshot:

- Orange Pi runtime: based on CollarPet v9.4.2 remote-style Pi UI
- Remote firmware: CollarPet remote v4.6.0 active gear
- Fan controller: CollarPet fan control v1

This repository now uses stable filenames; version history should be tracked with Git commits/tags rather than putting version numbers in every source filename.
